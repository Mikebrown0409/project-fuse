[package]
name = "fuse-tests"
version = "1.1.0"
edition = "2021"

[dependencies]
fuse-core = { path = "../fuse-core" }
fuse-cli = { path = "../fuse-cli" }
serde_json = "1.0"
anyhow = "1.0"
hex = "0.4"
sha2 = "0.10"
chrono = { version = "0.4", features = ["serde"] }
